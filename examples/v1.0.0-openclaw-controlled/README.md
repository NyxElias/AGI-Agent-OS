# v1.0.0 OpenClaw Controlled Example Pack

This directory provides:

- `*.example.*` for concise reference snippets
- `workspace-template/` for runnable controlled setup
- `workspace-template-invalid/` for validator failure-path tests
- `VALIDATION-EXPECTATIONS.md` for CI assertions
